## revkeen prices list

List prices

```
revkeen prices list [flags]
```

### Options

```
      -- string                 
      --active string           Filter by active status
      --currency string         Filter by currency
      --ending_before string    Cursor for pagination (ID to end before)
  -h, --help                    help for list
      --limit int               Number of results (1-100)
      --lookup_key string       Filter by lookup key
      --pricing_model string    Filter by pricing model
      --product_id string       Filter by product ID
      --starting_after string   Cursor for pagination (ID to start after)
      --type string             Filter by price type
```

### Options inherited from parent commands

```
      --agent            Machine-readable mode: compact JSON, no color, errors as JSON to stderr
      --api-key string   Override API key (or set REVKEEN_API_KEY)
      --json             Shorthand for --output json (pretty-printed)
      --no-color         Disable color output
  -o, --output string    Output format: table, json, yaml, csv (default "table")
      --table            Shorthand for --output table
```

### SEE ALSO

* [revkeen prices](revkeen_prices.md)	 - Operations on prices

